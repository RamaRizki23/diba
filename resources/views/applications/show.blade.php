@extends('layouts.app')
@section('content')
<div class="topbar"><div><div class="eyebrow">Daftar aplikasi / detail</div><h1 class="page-title">{{ $application->name }}</h1></div><div class="top-actions"><a class="button secondary" href="{{ route('applications.index') }}">Kembali</a><a class="button" href="{{ route('applications.pdf', $application) }}">Unduh PDF</a></div></div>
<div class="panel"><div class="panel-head"><div><div class="code">{{ $application->code }}</div><h2 style="margin-top:5px">Informasi aplikasi</h2></div><span class="status {{ strtolower(str_replace(' ', '-', $application->status)) }}">{{ $application->status }}</span></div>
<div class="form-grid"><div><label>Nama pemilik</label><div>{{ $application->owner ?: '-' }}</div></div><div><label>Layanan</label><div>{{ $application->service ?: '-' }}</div></div><div><label>Tahun pembuatan</label><div>{{ $application->year ?: '-' }}</div></div><div><label>Sektor</label><div>{{ $application->sector ?: '-' }}</div></div><div><label>URL aplikasi</label><div>@if($application->url)<a style="color:var(--teal)" href="{{ $application->url }}" target="_blank">{{ $application->url }}</a>@else - @endif</div></div><div><label>Server</label><div>{{ $application->server ?: '-' }}</div></div><div><label>Bahasa pemrograman</label><div>{{ $application->language ?: '-' }}</div></div><div><label>Framework</label><div>{{ $application->framework ?: '-' }}</div></div><div><label>Database</label><div>{{ $application->database ?: '-' }}</div></div><div><label>Operating system</label><div>{{ $application->operating_system ?: '-' }}</div></div><div class="full"><label>Deskripsi</label><div class="muted">{{ $application->description ?: '-' }}</div></div><div><label>Unit operasional</label><div class="muted">{{ $application->operational_unit ?: '-' }}</div></div><div><label>Aplikasi terintegrasi</label><div class="muted">{{ $application->integrations ?: '-' }}</div></div><div><label>Biaya pembangunan</label><div>Rp {{ number_format((float) $application->development_cost, 0, ',', '.') }}</div></div></div></div>
@endsection
